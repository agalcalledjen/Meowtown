const styles = theme => ({
  root: {
    flexGrow: 1,
    padding: theme.spacing.unit * 12
  },
  // paper: {
  //   height: 140,
  //   width: 100
  // },
  control: {
    padding: theme.spacing.unit * 2
  }
});

export default styles;
