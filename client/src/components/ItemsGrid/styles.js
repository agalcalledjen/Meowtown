const styles = theme => ({
  itemsRoot: {
    flexGrow: 1,
    padding: theme.spacing.unit * 12,
    paddingTop: 0,
    margin: 0
  },
  control: {
    padding: theme.spacing.unit * 2
  }
});

export default styles;
