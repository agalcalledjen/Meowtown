const styles = theme => ({
  itemsRoot: {
    flexGrow: 1,
    padding: theme.spacing.unit * 12,
    paddingTop: 0
  },
  // paper: {
  //   height: 140,
  //   width: 100
  // },
  control: {
    padding: theme.spacing.unit * 2
  }
});

export default styles;
